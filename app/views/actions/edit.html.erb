<div class="max-w-2xl mx-auto py-8">
  <h1 class="text-2xl font-bold mb-6">Edit Action</h1>
  <%= render "form", action: @action %>
  <div class="mt-4">
    <%= link_to "Back to Action", case_action_path(@case, @action), class: "text-blue-600 hover:underline" %>
  </div>
</div>
