<%= form_with(model: [@case, action], scope: :action_record) do |form| %>
  <% if action.errors.any? %>
    <div class="notification-error">
      <h2><%= pluralize(action.errors.count, "error") %> prohibited this action from being saved:</h2>
      <ul class="list-disc ml-5 mt-2">
        <% action.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title, class: "form-label" %>
    <%= form.text_field :title, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :description, class: "form-label" %>
    <%= form.text_area :description, rows: 3, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :action_type, class: "form-label" %>
    <%= form.select :action_type, Action.action_types.keys.map { |t| [t.humanize, t] }, { prompt: "Select type" }, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :status, class: "form-label" %>
    <%= form.select :status, Action.statuses.keys.map { |s| [s.humanize, s] }, {}, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :due_date, class: "form-label" %>
    <%= form.datetime_local_field :due_date, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :blocked_by, class: "form-label" %>
    <%= form.text_field :blocked_by, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :policy_reference_id, "Policy Reference", class: "form-label" %>
    <%= form.collection_select :policy_reference_id, PolicyReference.all, :id, :title, { prompt: "None", include_blank: true }, class: "w-full" %>
  </div>

  <div class="form-group">
    <%= form.label :caseworker_guidance, class: "form-label" %>
    <%= form.text_area :caseworker_guidance, rows: 3, class: "w-full" %>
  </div>

  <div>
    <%= form.submit class: "btn-primary cursor-pointer" %>
  </div>
<% end %>
