<div class="max-w-2xl mx-auto py-8">
  <h1 class="text-2xl font-bold mb-6">New Action for Case: <%= @case.reference %></h1>
  <%= render "form", action: @action %>
  <div class="mt-4">
    <%= link_to "Back to Actions", case_actions_path(@case), class: "link-back" %>
  </div>
</div>
