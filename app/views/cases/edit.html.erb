<div class="max-w-2xl mx-auto py-8">
  <h1 class="text-2xl font-bold mb-6">Edit Case: <%= @case.reference %></h1>
  <%= render "form" %>
  <div class="mt-4">
    <%= link_to "Back to Case", @case, class: "text-blue-600 hover:underline" %>
  </div>
</div>
