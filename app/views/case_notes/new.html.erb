<div class="max-w-2xl mx-auto py-8">
  <h1 class="text-2xl font-bold mb-6">New Note for Case: <%= @case.reference %></h1>
  <%= render "form", case_note: @case_note %>
  <div class="mt-4">
    <%= link_to "Back to Notes", case_case_notes_path(@case), class: "text-blue-600 hover:underline" %>
  </div>
</div>
